[package]
name = "cxpak"
version = "3.1.4"
edition = "2021"
rust-version = "1.91"
description = "Spends CPU cycles so you don't spend tokens. The LLM gets a briefing packet instead of a flashlight in a dark room."
license = "MIT OR Apache-2.0"
repository = "https://github.com/Barnett-Studios/cxpak"
keywords = ["llm", "context", "tree-sitter", "cli", "ai"]
categories = ["command-line-utilities", "development-tools"]
# Maintainer-local config (rustflags that flip on the `dev_maintenance` cfg)
# never reaches crates.io.  `cargo install cxpak` ignores local config anyway
# (see doc.rust-lang.org/cargo/commands/cargo-install.html "Configuration
# Discovery"), but stripping the dir from the tarball keeps source-browsers
# from being misled.
exclude = [".cargo", "vendor"]

[dependencies]
clap = { version = "4", features = ["derive"] }
tree-sitter = "0.25"
tree-sitter-rust = { version = "0.24", optional = true }
tree-sitter-typescript = { version = "0.23", optional = true }
tree-sitter-javascript = { version = "0.23", optional = true }
tree-sitter-java = { version = "0.23", optional = true }
tree-sitter-python = { version = "0.23", optional = true }
tree-sitter-go = { version = "0.23", optional = true }
tree-sitter-c = { version = "0.24", optional = true }
tree-sitter-cpp = { version = "0.23", optional = true }
tiktoken-rs = "0.6"
git2 = { version = "0.20", default-features = false }
ignore = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1"
tree-sitter-ruby = { version = "0.23", optional = true }
tree-sitter-c-sharp = { version = "0.23", optional = true }
tree-sitter-swift = { version = "0.7", optional = true }
tree-sitter-kotlin-ng = { version = "1.1.0", optional = true }
tree-sitter-bash = { version = "0.25", optional = true }
tree-sitter-css = { version = "0.25", optional = true }
tree-sitter-scss = { version = "1.0", optional = true }
tree-sitter-php = { version = "0.24", optional = true }
tree-sitter-markdown-updated = { version = "0.1", optional = true }
tree-sitter-json = { version = "0.24", optional = true }
tree-sitter-yaml = { version = "0.7", optional = true }
tree-sitter-toml-updated = { version = "0.20", optional = true }
tree-sitter-dockerfile-updated = { version = "0.2", optional = true }
tree-sitter-hcl = { version = "1.1", optional = true }
tree-sitter-dart = { version = "0.1", optional = true }
tree-sitter-scala = { version = "0.25", optional = true }
tree-sitter-lua = { version = "0.5", optional = true }
tree-sitter-clojure = { version = "0.1.0", optional = true }
tree-sitter-elixir = { version = "0.3", optional = true }
tree-sitter-zig = { version = "1.1", optional = true }
tree-sitter-haskell = { version = "0.23", optional = true }
tree-sitter-groovy = { version = "0.1", optional = true }
tree-sitter-objc = { version = "3.0", optional = true }
tree-sitter-r = { version = "1.2", optional = true }
tree-sitter-julia = { version = "0.23", optional = true }
tree-sitter-ocaml = { version = "0.24", optional = true }
tree-sitter-matlab = { version = "1.3", optional = true }
tree-sitter-proto = { version = "0.4", optional = true }
tree-sitter-svelte-ng = { version = "1.0", optional = true }
tree-sitter-make = { version = "1.1", optional = true }
tree-sitter-html = { version = "0.23", optional = true }
tree-sitter-graphql = { version = "0.1", optional = true }
tree-sitter-xml = { version = "0.7", optional = true }
tree-sitter-sequel = { version = "0.3", optional = true }
tree-sitter-prisma-io = { version = "1.6", optional = true }
unicode-normalization = "0.1"
regex = "1"
chrono = { version = "0.4", features = ["serde"] }
notify = { version = "7", optional = true }
axum = { version = "0.8", optional = true }
tokio = { version = "1", features = ["full"], optional = true }
# cxpak#57: the official Rust client. Optional — see the `client` feature.
rmcp = { version = "2", features = ["client", "transport-child-process"], optional = true }
async-trait = { version = "0.1", optional = true }
# client.rs reaps a hung child by PID (SIGKILL) rather than leaking it; unix-only path.
libc = { version = "0.2", optional = true }
candle-core = { version = "0.9", optional = true }
candle-nn = { version = "0.9", optional = true }
candle-transformers = { version = "0.9", optional = true }
tokenizers = { version = "0.22", optional = true, default-features = false, features = ["onig"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "blocking", "rustls-tls"], optional = true }
bincode = { version = "1", optional = true }
tower-lsp = { version = "0.20", optional = true }
fs2 = "0.4"
sha2 = "0.10"
subtle = "2"
toml = { version = "0.8", optional = true }
resvg = { version = "0.47", optional = true }
petgraph = { version = "0.6", optional = true }
thiserror = { version = "2", optional = true }
wasmtime = { version = "43", optional = true }
# Live DB introspection (feature `data-introspect`, off by default).  All TLS is
# rustls — NO native-tls/openssl anywhere in the tree (ADR-0163, ADR-0173).
tokio-postgres = { version = "0.7", optional = true }
tokio-postgres-rustls = { version = "0.13", optional = true }
rustls = { version = "0.23", optional = true, default-features = false, features = ["ring"] }
mysql_async = { version = "0.37", optional = true, default-features = false, features = ["minimal", "rustls-tls"] }

# Global allocator (perf/watcher-allocator-debounce, Mechanism A). The default
# system allocator (macOS libmalloc / glibc) parks freed blocks in per-process
# free lists under the watcher's per-batch clone churn, so RSS ratchets to tens
# of GB and never returns. jemalloc with aggressive decay (`dirty_decay_ms` /
# `muzzy_decay_ms`, see `malloc_conf` in `src/main.rs`) purges those pages back
# to the OS, capping steady-state RSS near the live set. jemalloc does not build
# on MSVC, so the dep and the `#[global_allocator]` are both `cfg`-gated off it.
[target.'cfg(all(not(target_env = "msvc"), target_os = "linux"))'.dependencies]
tikv-jemallocator = { version = "0.6", features = ["unprefixed_malloc_on_supported_platforms", "background_threads"] }

# No `background_threads` off Linux. That cargo feature makes tikv-jemallocator
# define its OWN `malloc_conf` of `background_thread:true`, which jemalloc then
# rejects on macOS with
#   <jemalloc>: option background_thread currently supports pthread only
# printed to stderr on every start — and `cxpak serve --mcp` is a stdio server,
# so that lands in the client's logs every launch.
[target.'cfg(all(not(target_env = "msvc"), not(target_os = "linux")))'.dependencies]
tikv-jemallocator = { version = "0.6", features = ["unprefixed_malloc_on_supported_platforms"] }

[dev-dependencies]
# Self dev-dependency so integration tests (tests/*.rs) see `cxpak::test_support`
# with the `test-support` feature on, while the release build never enables it.
cxpak = { path = ".", features = ["test-support"] }
assert_cmd = "2"
predicates = "3"
tempfile = "3"
tower = { version = "0.5", features = ["util"] }
headless_chrome = "1"
tiny_http = "0.12"
proptest = "1"
criterion = "0.5"

[[bench]]
name = "indexing"
harness = false

[features]
default = [
    "lang-rust", "lang-typescript", "lang-javascript", "lang-java", "lang-python", "lang-go",
    "lang-c", "lang-cpp", "lang-ruby", "lang-csharp", "lang-swift", "lang-kotlin",
    "lang-bash", "lang-css", "lang-scss", "lang-php", "lang-markdown",
    "lang-json", "lang-yaml", "lang-toml", "lang-dockerfile", "lang-hcl", "lang-dart",
    "lang-scala", "lang-lua", "lang-clojure", "lang-elixir", "lang-zig", "lang-haskell",
    "lang-groovy", "lang-objc", "lang-r", "lang-julia", "lang-ocaml", "lang-matlab",
    "lang-proto", "lang-svelte", "lang-makefile", "lang-html", "lang-graphql", "lang-xml",
    "lang-sql", "lang-prisma",
    "daemon",
    "embeddings",
    "lsp",
    "visual",
    # `plugins` (WASM plugin loader) intentionally excluded from default.
    # PluginLoader::load() currently always errors with "guest function
    # binding not yet implemented" — shipping that as a default feature
    # made `cxpak plugin add` look functional while the loader was a
    # known stub.  Build with `--features plugins` once the WIT bridge
    # is wired up.  Existing plugin-management commands (`list`, `add`,
    # `verify` of `.cxpak/plugins.json`) move under the same gate.
]
lang-rust = ["dep:tree-sitter-rust"]
lang-typescript = ["dep:tree-sitter-typescript"]
lang-javascript = ["dep:tree-sitter-javascript"]
lang-java = ["dep:tree-sitter-java"]
lang-python = ["dep:tree-sitter-python"]
lang-go = ["dep:tree-sitter-go"]
lang-c = ["dep:tree-sitter-c"]
lang-cpp = ["dep:tree-sitter-cpp"]
lang-ruby = ["dep:tree-sitter-ruby"]
lang-csharp = ["dep:tree-sitter-c-sharp"]
lang-swift = ["dep:tree-sitter-swift"]
lang-kotlin = ["dep:tree-sitter-kotlin-ng"]
lang-bash = ["dep:tree-sitter-bash"]
lang-css = ["dep:tree-sitter-css"]
lang-scss = ["dep:tree-sitter-scss"]
lang-php = ["dep:tree-sitter-php"]
lang-markdown = ["dep:tree-sitter-markdown-updated"]
lang-json = ["dep:tree-sitter-json"]
lang-yaml = ["dep:tree-sitter-yaml"]
lang-toml = ["dep:tree-sitter-toml-updated"]
lang-dockerfile = ["dep:tree-sitter-dockerfile-updated"]
lang-hcl = ["dep:tree-sitter-hcl"]
lang-dart = ["dep:tree-sitter-dart"]
lang-scala = ["dep:tree-sitter-scala"]
lang-lua = ["dep:tree-sitter-lua"]
lang-clojure = ["dep:tree-sitter-clojure"]
lang-elixir = ["dep:tree-sitter-elixir"]
lang-zig = ["dep:tree-sitter-zig"]
lang-haskell = ["dep:tree-sitter-haskell"]
lang-groovy = ["dep:tree-sitter-groovy"]
lang-objc = ["dep:tree-sitter-objc"]
lang-r = ["dep:tree-sitter-r"]
lang-julia = ["dep:tree-sitter-julia"]
lang-ocaml = ["dep:tree-sitter-ocaml"]
lang-matlab = ["dep:tree-sitter-matlab"]
lang-proto = ["dep:tree-sitter-proto"]
lang-svelte = ["dep:tree-sitter-svelte-ng"]
lang-makefile = ["dep:tree-sitter-make"]
lang-html = ["dep:tree-sitter-html"]
lang-graphql = ["dep:tree-sitter-graphql"]
lang-xml = ["dep:tree-sitter-xml"]
lang-sql = ["dep:tree-sitter-sequel"]
lang-prisma = ["dep:tree-sitter-prisma-io"]
daemon = ["dep:notify", "dep:axum", "dep:tokio"]
lsp = ["dep:tower-lsp", "daemon"]
embeddings = [
    "dep:candle-core", "dep:candle-nn", "dep:candle-transformers",
    "dep:tokenizers", "dep:reqwest", "dep:bincode",
]
visual = ["dep:resvg", "dep:petgraph", "dep:thiserror"]
e2e-browser = []
# cxpak#57: NON-default by design. cxpak had zero rmcp before this; #32 already records
# the default feature set as a kitchen sink, so the client must not ride into
# `cargo install cxpak`. tests/client_feature_boundary.rs is empty without it.
client = ["dep:rmcp", "dep:async-trait", "dep:tokio", "dep:libc"]
plugins = ["dep:wasmtime"]
# Test-only helpers (`cxpak::test_support`). Off by default → never compiled
# into a release binary. Integration tests receive it through the self
# dev-dependency below, unit tests through `cfg(test)`.
test-support = []
# Live DB introspection: rustls-only Postgres + MySQL drivers + a scoped tokio
# runtime.  Off by default — keeps the default build OpenSSL-free AND DB-free.
data-introspect = [
    "dep:tokio", "dep:tokio-postgres", "dep:tokio-postgres-rustls",
    "dep:rustls", "dep:mysql_async",
]
bench = ["dep:toml"]

# Optional local reranker (D1, ADR-0184): a deterministic, no-LLM lexical
# cross-encoder that re-orders the top-N fused candidates after RRF fusion.
# NON-default and OFF by default — excluded from the determinism fixture; the
# core ranking is fully deterministic without it. No new dependencies (a
# model-backed cross-encoder would live behind THIS flag but needs a heavy
# native runtime, deferred per ADR-0163 / the D1 stop-and-ask rule).
reranker = []

# `dev_maintenance` cfg is flipped on by the project-local .cargo/config.toml
# via rustflags during maintainer builds.  Whitelist it so rustc's
# unexpected_cfgs lint (default-warn since 1.80) doesn't fire on
# src/dev_maintenance.rs.  Crates.io users never have the cfg set — their
# `cargo install cxpak` ignores local config entirely — so the gated body
# compiles out with no symbols in the resulting binary.
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(dev_maintenance)"] }

# ─────────────────────────────────────────────────────────────────────────────
# Disk-usage controls.  cxpak depends on wasmtime + candle + 43 tree-sitter
# parsers; without these overrides the ~50 integration-test binaries each
# carry full debug info for every linked dep, and `target/` reaches 100 GB+
# in a busy session.
#
# `[profile.dev]` keeps line-table debug on cxpak's own code (back-traces
# still resolve to file:line) but strips full debug info — typical 40-50%
# binary-size reduction.  `[profile.dev.package."*"]` strips ALL debug info
# from third-party deps, where step-debugging is rarely useful and the
# weight is dominant.  `[profile.test]` inherits both.
# ─────────────────────────────────────────────────────────────────────────────

[profile.dev]
debug = "line-tables-only"
incremental = true
split-debuginfo = "unpacked"

[profile.dev.package."*"]
debug = false

# Tests inherit profile.dev settings; explicit declaration here documents the
# choice and lets future tuning (e.g., `opt-level = 1` for slow test
# fixtures) attach without surprise.
[profile.test]
debug = "line-tables-only"

# ─────────────────────────────────────────────────────────────────────────────
# Vendored patches for the x86_64-pc-windows-msvc build.  Both crates pass a
# GCC/Clang-only flag to the compiler via cc's raw `.flag()`, which MSVC's
# cl.exe rejects — breaking the Windows build.  Each vendored copy is byte-
# identical except `.flag()` -> `.flag_if_supported()` (kept on GCC/Clang where
# the flag is valid, probed-and-dropped on MSVC).  Remove once fixed upstream.
# Excluded from the published crate (see `exclude` above).
#   - tree-sitter-scss 1.0.0: `-Wno-unused-parameter` (cl error D8021)
#   - esaxx-rs 0.1.10:        `-std=c++11` (ungated for non-macOS; cl defaults
#                             to c++14, a superset, so dropping it is safe)
# ─────────────────────────────────────────────────────────────────────────────
#
# tree-sitter-markdown-updated 0.1.0 is vendored for a different reason: its
# external scanner writes the parse state into tree-sitter's 1024-byte
# serialization buffer BEFORE testing that it fits, so a sufficiently nested or
# delimiter-dense markdown file overruns that buffer.  The grammar is built with
# TREE_SITTER_MARKDOWN_AVOID_CRASH, which turns the (too-late) bounds assert
# into `throw 1` — and the scanner is `extern "C"`, so that exception cannot
# cross back into Rust and the process dies in std::terminate.  One markdown
# file therefore killed `cxpak serve` at startup with no message naming it.
# The vendored copy makes "the state always fits" an invariant: the scanner
# refuses to push state it could not serialize, so the bound is enforced before
# anything is written.  See vendor/tree-sitter-markdown-updated/PATCH.md.
# Upstream (github.com/c-gamble/tree-sitter-markdown) has issues disabled and
# 0.1.0 is its only crates.io release.
# ─────────────────────────────────────────────────────────────────────────────
[patch.crates-io]
tree-sitter-scss = { path = "vendor/tree-sitter-scss" }
esaxx-rs = { path = "vendor/esaxx-rs" }
tree-sitter-markdown-updated = { path = "vendor/tree-sitter-markdown-updated" }
